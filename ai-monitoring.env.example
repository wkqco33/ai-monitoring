# Azure OpenAI 설정
AZURE_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_API_KEY=your-api-key-here
AZURE_DEPLOYMENT=gpt-4o

# 추가적인 CLI 플래그를 환경 변수로 넘기고 싶다면 ExecStart에 추가하거나 
# 필요시 config.go 등을 수정하여 환경변수를 직접 읽게 할 수 있습니다.
# 현재는 start.go에서 os.Getenv를 통해 이 변수들을 읽고 있습니다.
