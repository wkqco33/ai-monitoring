# 기여 가이드 (Contributing)

ai-monitoring 프로젝트에 기여해 주셔서 감사합니다. 아래 가이드를 읽고 기여해 주세요.

## 개발 워크플로우 (TDD)

이 프로젝트는 **TDD(테스트 주도 개발)** 방식을 따릅니다.

1. 기능 또는 버그 수정을 위한 실패하는 테스트를 먼저 작성합니다.
2. 테스트가 통과하도록 최소한의 코드를 구현합니다.
3. 구현 후 관련 테스트를 리팩터링합니다.

작업을 시작하기 전에 `AGENTS.md`의 개발 가이드를 반드시 확인해 주세요.

## 로컬 개발

```bash
# 의존성 설치
task tidy

# 테스트 실행
task test

# 코드 포맷팅
task fmt

# 정적 분석
task vet
```

커밋 전에 위의 `task test`, `task fmt`, `task vet`를 모두 통과해야 합니다.

## 이슈 / PR 절차

1. 작업할 내용을 명확히 설명하는 이슈를 먼저 등록합니다.
2. `master` 브랜치에서 작업용 브랜치를 생성합니다.
3. 한 브랜치에는 하나의 논리적 변경만 담습니다.
4. 커밋 메시지는 변경 내용을 간결하고 명확하게 작성합니다.
5. PR을 열 때 변경 사항과 테스트 결과를 설명합니다.

## 코드 스타일

- Go 표준 `gofmt` 포맷을 따릅니다.
- 주석은 "무엇"보다 "왜"를 설명하는 데 사용하며, 불필요한 설명은 제거합니다.
- 패키지/함수/타입에는 관용적인 Go doc 주석을 사용합니다.

## 테스트 작성 지침

- 핵심 로직(`analyzer`, `monitor`, `config`, `notifier` 등)에는 단위 테스트를 작성합니다.
- 외부 시스템(LLM API, OS 알림)은 목(mock) 또는 경계가 명확한 함수로 분리해 테스트 가능하게 유지합니다.
- 테스트는 결정적(deterministic)이어야 하며 호스트 환경의 영향(예: 환경변수)을 받지 않아야 합니다.
